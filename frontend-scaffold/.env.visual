# Build-time environment for the visual regression suite (vite --mode visual).
# Loaded on top of .env so screenshots never depend on network data.
VITE_USE_MOCK_DATA=true
VITE_CONTRACT_ID=CAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
VITE_NETWORK=TESTNET
VITE_SENTRY_DSN=
